import localStrategy from './../../../../src/server/controllers/auth/local.strategy.conf';

describe('local.strategy.conf', () => {
    it('should exists', () => expect(localStrategy).toBeDefined());
});
