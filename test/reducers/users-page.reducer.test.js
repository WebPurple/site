import { expect } from 'chai';

import usersPageReducer from './../../src/reducers/users-page.reducer';

describe('users-page.reducer', () => {
    it('should exists', () => expect(usersPageReducer).to.be.defined);
});
